services:
  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    container_name: bookmark_saver_keycloak
    command: start
    depends_on:
      keycloak-db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/${KEYCLOAK_POSTGRES_DB}
      KC_DB_USERNAME: ${KEYCLOAK_POSTGRES_USER}
      KC_DB_PASSWORD: ${KEYCLOAK_POSTGRES_PASSWORD}
      KC_HOSTNAME: ${KEYCLOAK_HOSTNAME}
      KC_HEALTH_ENABLED: "true"
      KC_HTTP_ENABLED: "true" # remove if you terminate TLS directly on keycloak
      # KC_PROXY_HEADERS: xforwarded # reverse proxy/nginx
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:9000/health/ready || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 6
      start_period: 60s
    ports:
      - "8180:8080"

  keycloak-db:
    image: postgres:16
    container_name: bookmark_saver_keycloak_db
    environment:
      POSTGRES_USER: ${KEYCLOAK_POSTGRES_USER}
      POSTGRES_PASSWORD: ${KEYCLOAK_POSTGRES_PASSWORD}
      POSTGRES_DB: ${KEYCLOAK_POSTGRES_DB}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${KEYCLOAK_POSTGRES_USER} -d ${KEYCLOAK_POSTGRES_DB}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - keycloak_postgres_data:/var/lib/postgresql/data

volumes:
  keycloak_postgres_data:
